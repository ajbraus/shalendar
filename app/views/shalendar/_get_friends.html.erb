<div class="part get_friends">
	<div id="view_search">
		<p>Find Friends</p>
		<%= form_tag({:controller => 'shalendar', :action => 'search'}, {id: 'search_form', remote: true }) do %>
		  <%= text_field_tag :search, params[:search], id: "user_search_field", class: "search_spinner", placeholder:"Search by Full Name or Email", autocomplete: "off" %>
		  <input type="submit" id="btn_user_search" class="btn-success fa-input btn-user-search" value="&#xf002; Find Friends">
		<% end %> 
	</div>
	<div class="social_friends">
		<% if session[:access_token] %>
			<%= link_to find_friends_path, :remote => true, class: "find_friends fancybox.ajax" do %>
				<%= image_tag "social/fb.png", :size => "50x50" %>
			<% end %>
		<% else %>
			<div class="fb_button">
				<div id="auth-status">
		    	<a href="#" id="auth-loginlink" class="fb-login-button" ><%= image_tag "social/fb.png", :size => "50x50" %></a>
				  </div>
			</div><br>
		<% end %>
	</div>
</div>