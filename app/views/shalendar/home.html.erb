<aside>
  <%= button_to "New", new_event_path, :class => 'btn btn-large btn-primary', :method => :get %>

  <div style="height:140px;"><%= medium_profile_picture(@current_user) %></div>

  <%= render 'stats' %>

  <%= render 'search' %>

  <%= render 'view_requests' %>

  <%= render 'raster' %>
  
  <div class="social_friends">
  <%= link_to( (image_tag "fb.png", :size => "50x50"), find_friends_path) %>
  </div>
</aside>

 <div id='calendar'></div>
