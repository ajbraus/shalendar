//change the check to blue (toggleRsvpCheck.js.coffee)

// rsvp to instances in calendar 
	time = $('.instance').withEventId(<%= @event.id %>)
	// time.rsvp_check("<%= escape_javascript(render partial: 'shalendar/rsvp_check', object: @event, as: :event) %>", '' ); 
	time.find('.mini_in_out').first().html("<div class='in_on_instance'><i class='icon-ok blue' title='you're .in'></i> .in</div>")
	time.find('a').first().removeClass('not_rsvpd');

// rsvp to parent in .invites
<% @parent = @event.parent %>
<% if @parent.present? %>	
	parent = $('.event').withEventId(<%= @event.id %>)
	parent.find('.out').css("color", "lightgrey")
	parent.find('.in_check').css("color", "#CD0000");
	parent.find('#status').html(".interested")
<% end %>

// rsvp to one_time in calendar
<% if @event.one_time? %>
	<% @event.instances.each do |i| %> 
		time = $('.instance').withEventId(<%= i.id %>)
		time.find('.mini_in_out').first().html("<div class='in_on_instance'><i class='icon-ok blue' title='you're .in'></i> .in</div>")
		time.find('a').first().removeClass('not_rsvpd');

		//if instances weren't there - but they are
		//$('.day_container').withDate(<%= "#{i.starts_at.to_date}" %>)
		//day.append("<%= escape_javascript (render partial: 'shalendar/mini_event', object: i, as: :event) %>")
	<% end %>
<% end %>

event = $('div').withEventId(<%= @event.id %>)
event.find('.in_check').click(function() {
	$(this).preventDefault();
});



//if I want to make event#show ajaxy
//$('#rsvpForm').html("<%= escape_javascript(render partial: 'events/rsvp_form', object: @event, as: :event) %>")

var afterRsvpLightbox = $('#shareAfterRsvp')
afterRsvpLightbox.html("<%= escape_javascript(render partial: 'shared/share_after_rsvp', object: @event, as: :event) %>")
afterRsvpLightbox.fadeIn();
$('#overlay').fadeIn();

$('#close').click(function(){
	afterRsvpLightbox.fadeOut();
	$('#overlay').fadeOut();
});

$('body').click(function() {
  afterRsvpLightbox.fadeOut();
  $('#overlay').fadeOut();
});

$('.share_after_rsvp').children().click(function(event){
     event.stopPropagation();
 });

// $("a[href$='new_invited_events']").click(function(event){
//      event.stopPropagation();
//      $('#new_invited_events_count').text('0').hide();
// });

