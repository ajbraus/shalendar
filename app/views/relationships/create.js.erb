$('.follow_form_hook').withUserId(<%= @user.id %>).html("<%= escape_javascript( render partial: 'events/follow_form', locals: { :user => @user } ) %>");
$('#header_friend_count').increment();
$('#header_inmate_count').decrement();