<header>
  <% if user_signed_in? %>
  <%= link_to "Calenshare", '/home' %>
  <% else %>
  <%= link_to "Calenshare", '/' %>
  <% end %>
  <nav>
      <% if user_signed_in? %>
      <ul>
        <li> <%= link_icon("",edit_user_registration_path,"wrench","1","") %></li>
        <li><%= link_to "Logout", destroy_user_session_path, :method => :delete, id: 'auth-logoutlink' %></li>
      </ul>
      <% end %>
  </nav>
</header>