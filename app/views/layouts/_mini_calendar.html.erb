<nav class="clearfix">
   <%= render 'layouts/logo' %>
    <div class="city_name" id="cityName"><% if @current_city.present? %> <%= @current_city.name %><%= link_to "move", pick_city_path, id:"editCity", style:"display:none;" %><% end %></div>
    <div class="navigation">
      <% if user_signed_in? %>
	      <% if current_user.admin? %>
	        <%= link_to admin_dashboard_path, style:"position: relative;bottom: 7px;" do %>
	          <span class="fadeInRed"><i class="icon-magic"></i></span>
	        <% end %>
	      <% end %>
      <% end %>
    </div>
</nav>
  
<%= render 'users/profile' %>
<div class="search_new_button">
<% if user_signed_in? %>
	<%= render 'shared/friend_search' %>
	<%= render 'shared/new_idea_button' %>
<% end %>
</div>
<div class="navbars-set">
<% if user_signed_in? && @user == current_user %>
	<div class="navbar active hover-purple" id="showInvitedIdeas">
		<i class="icon-cloud"></i><span id="invited_ideas_count"> <%= @invited_ideas.count %></span> ideas</span> <i class="icon-caret-right"></i> 
	</div>
	<div class="navbar hover-orange" id="showInvitedTimes">
		<i class="icon-calendar"></i><span id="invited_times_count"> <%= @invited_times_count %></span> .invites <%= image_tag 'grey_spinner.gif', class:"grey_spinner", id:"invitesSpinner" %></span>
	</div>
<% end %>
	<div class="navbar hover-blue" id="showInteresteds">
		<i class="icon-cloud"></i><i class="icon-ok"></i><span id="interesteds_count"> <%= @interesteds_count %></span> .interested  <% if user_signed_in? && @user == current_user %> <%= image_tag 'grey_spinner.gif', class:"grey_spinner", id:"interestedSpinner" %> <% end %>
	</div>
<% if user_signed_in? && (@user == current_user || @user.is_inmates_or_friends_with?(current_user)) %>
	<div class="navbar hover-blue" id="showIns">
		<i class="icon-calendar"></i><i class="icon-ok"></i><span id="ins_count"> <%= @ins_count %></span> .ins  <%= image_tag 'grey_spinner.gif', class:"grey_spinner", id:"insSpinner" %>
	</div>
<% end %>
<!-- 	<div class="navbar hover-dark-blue" id="showOvers">
		<i class="icon-reply"></i> overs
	</div> -->

	<div class="upcoming-times clearfix" id="upcomingTimes">
	</div>

<% if false %>
<% if @user == current_user %>
	<div class="navbar hover-red" id="showOuts">
		<i class="icon-remove"></i> outs
	</div>
<% end %>
<% end %>

		<div class="navbar hover-green" id="help">
			<i class="icon-question-sign"></i> help
		</div>
	<% if user_signed_in? %>
	<%= link_to edit_user_registration_path do %>
		<div class="navbar hover-light-orange">
      <i class='icon-cog user_settings'></i> settings
    </div>
  <% end %>
  <%= link_to destroy_user_session_path, :method => :delete do %>
  	<div class="navbar hover-red">
      <i class='icon-signout logout'></i> logout
    </div>
  <% end %>
  <% end %>
</div>
<%= render 'layouts/footer' %>