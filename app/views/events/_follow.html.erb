<%= form_for current_user.relationships.build(followed_id: user.id) do |f| %>
  <div><%= f.hidden_field :followed_id %></div>
  <button class="btn btn-follow rsvp_button" name="commit" type="submit" title="make <%= user.first_name %> your friend">
  	<i class="icon-star"></i>
  </button>

<% end %>