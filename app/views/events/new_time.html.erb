<div class="new_crowd_idea clearfix">
	<div class="event_form">
	  <%= form_for @parent.instances.build(title: @parent.title, address: @parent.address), url: { action: 'create_new_time' },
								 id: "new_event_form" do |f| %>
	  <p>		
	  	<div class="title_char_counter"><span id="titleCharCount">0</span>/60</div>
		  <%= label :title, "Idea*" %><br/>
	  	<%= f.text_field :title, id: "new_idea_title", class: "required disabled", autocomplete: "off", disabled:true %>
		</p>
  	<div id="dateTime">
		  <div class="starts_at">
		    <%= label :starts_at, "When does it start?*" %>
		    <%= f.text_field :chronic_starts_at, id: "datetime", class: "starts_at required", autocomplete: "off" %>
		  </div>
		  <div class="duration">
		    <%= label :duration, "How Long?*" %>
		    <%= f.text_field :duration, placeholder:"hrs", autocomplete: "off", class:"required"  %>
		  </div>
		</div>

	  <div>
	  	<%= label :description, "Details" %><br/>
	  	<%= f.text_area :description, id: "new_idea_description", autocomplete: "off", onkeypress: "if(event.keyCode==13){return false;}" %>
	  </div> 

		<div id="addmapp">
	  	<%= label_tag "Location" %><br/>
	  	<i class="icon-map-marker"></i><%= f.text_field :address, autocomplete: "off" %>
		</div>

		<div id="addtippingp" class="minmax">
		  	<i class="icon-tasks"></i>
		    <%= f.label :min_attendees, "Tipping Point" %> <br/>
		    <%= f.select :min, 1..1000 %>
		</div>
		
		<div id="addimgp" style="display:none;">
	  	<div><%= f.label :display_image %></div>
	 	  	<i class="icon-link"></i><%= f.text_field :promo_url, autocomplete: "off", placeholder: "Image URL" %><br>
	  or<br>
	  		<i class="icon-camera-retro"></i><%= f.file_field :promo_img %>
		</div>
     <div>.invite . . .</div>
      <div class="invite_radios">
        <div>
          <%= radio_button_tag :invite_me, "1" %>
          <%= label_tag :invite_me_1 do %>
            <i class="icon-bullhorn questionMark"></i> everyone in <%= @current_city.name.split(",")[0].to_s.capitalize %>
          <% end %>
        </div>
        <div>
          <%= radio_button_tag :invite_me, "2", checked: true %>
          <%= label_tag :invite_me_2 do %>
            <i class="icon-group questionMark"></i> all my friends
          <% end %>
        </div>
        <div>
          <%= radio_button_tag :invite_me, "3" %>
          <%= label_tag :invite_me_3 do %>
            <i class="icon-remove questionMark"></i> let me pick
          <% end %>
        </div>
      </div>		<%= f.hidden_field :parent_id, value: @parent.id %>
   
	  <span class="new_idea_button"><%= f.submit 'Add This Time', :class => 'btn btn-large btn-success', method: :post %></span>

	  <% end %>
	</div>
	<!-- PREVIEW -->
	<div class="idea_preview">
		<%= render partial: 'sample_idea' %>
	</div>
</div>