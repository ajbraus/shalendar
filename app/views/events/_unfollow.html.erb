<%= form_for(current_user.relationships.find_by_followed_id(user.id),
         html: { method: :delete }) do |f| %>
	<button class="btn btn-unfollow" name="commit" type="submit" title="Unstar <%= user.first_name %>">
		<i class="icon-star"></i>
	</button>
<% end %>