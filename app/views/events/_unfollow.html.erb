<%= form_for(current_user.relationships.find_by_followed_id(user.id),
         html: { method: :delete }) do |f| %>
	<button class="btn btn-unfollow" name="commit" type="submit" title="remove <%= user.first_name %> from your .inner-circle">
		<i class="icon-circle-blank"></i>
	</button>
<% end %>