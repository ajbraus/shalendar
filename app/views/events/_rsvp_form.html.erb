<div id="rsvp_form">
	<table id="social">
		<tr>
			<td>
				<% if current_user.rsvpd?(@event) %>
			  	<%= render 'unrsvp' %>
				<% elsif @event.full? %>
					Event is full :o(
				<% else %>
			  	<%= render 'rsvp' %>
				<% end %>
			</td>
			<td>
				<a href="https://twitter.com/share" class="twitter-share-button" data-text="Follow the link to RSVP for an event. Generated by @calenshare #realspace" data-size="large" data-count="none">Tweet</a>
			</td>
			<td>
				<div class="fb-like" data-send="false" data-width="450" data-show-faces="false" data-font="arial"></div>
			</td>
		</tr>
	</table>
	</div>
</span>